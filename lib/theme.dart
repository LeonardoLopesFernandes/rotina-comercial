import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFFE60014);
  static const primaryDark = Color(0xFFC50010);
  static const success = Color(0xFF2E7D32);
  static const successLight = Color(0xFF4CAF50);
  static const background = Color(0xFFF2F4F8);
  static const card = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1D1B20);
  static const textSecondary = Color(0xFF49454F);
  static const textMuted = Color(0xFF757575);
  static const textHint = Color(0xFF9E9E9E);
  static const divider = Color(0xFFE8E8E8);
  static const metricsBg = Color(0xFFF9FAFB);
  static const statActiveBg = Color(0xFFFEE2E2);
  static const statActiveText = Color(0xFFE31837);
  static const danger = Color(0xFFD32F2F);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  static const border = Color(0xFFDADCE0);
  static const buttonPrimary = Color(0xFFF3010B);
  static const buttonSecondary = Color(0xFFF1F3F4);
  static const buttonDisabled = Color(0xFF9E9E9E);
}

class AppText {
  static const xs = 10.0;
  static const sm = 12.0;
  static const md = 14.0;
  static const lg = 16.0;
  static const xl = 18.0;
  static const xxl = 20.0;
}

class AppRadius {
  static const sm = 6.0;
  static const md = 10.0;
  static const lg = 14.0;
  static const xl = 16.0;
}

const String store = 'L291';

const String blockedMessage =
    'Tratamento de itens bloqueado aguardando atualização dos dados de estoque e venda.';
